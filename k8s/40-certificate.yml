apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wisecow-tls
  namespace: wisecow
spec:
  secretName: wisecow-tls-secret
  issuerRef:
    name: wisecow-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - ec2-54-92-157-144.compute-1.amazonaws.com